# Entorno de DESARROLLO y TESTS del backend.
# Trae las deps de la función (raíz) + herramientas de test/lint.
# Instalar:  pip install -r backend/requirements.txt   (dentro de backend/.venv)

-r ../requirements.txt

pytest>=8.2,<9.0
pytest-asyncio>=0.24,<0.26
httpx>=0.27,<0.29
ruff>=0.6,<0.13
